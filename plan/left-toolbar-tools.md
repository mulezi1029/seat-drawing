# 左侧工具栏工具扩展计划

## 当前工具分析

### PanelLeft.tsx 已有工具
| 工具 | ID | 快捷键 | 分组 |
|------|-----|--------|------|
| 选择工具 | select | V | 选择组 |
| 选座工具 | select-seats | X | 选择组 |
| 选择刷 | brush | C | 选择组 |
| 焦点工具 | focal | F | 绘制组 |
| 矩形区域 | section | S | 绘制组 |
| 圆桌工具 | table | E | 绘制组 |
| 矩形区域(旋转) | area | G | 绘制组 |
| 平移工具 | hand | Space | 视图组 |

## 缺失的工具

### 1. 座位绘制工具 (SeatTool 类型中已定义)
| 工具 | ID | 快捷键 | 说明 |
|------|-----|--------|------|
| 单个座位 | single |  | 逐个点击添加座位 |
| 行座位 | row |  | 拖拽创建一行座位 |
| 线座位 | line |  | 沿路径创建座位 |

### 2. 多边形绘制工具
| 工具 | ID | 快捷键 | 说明 |
|------|-----|--------|------|
| 多边形区域 | polygon |  | 逐点点击创建任意多边形 |
| 椭圆工具 | ellipse |  | 创建椭圆形区域 |

### 3. 编辑工具
| 工具 | ID | 快捷键 | 说明 |
|------|-----|--------|------|
| 节点编辑 | edit-nodes |  | 编辑区域顶点 |
| 测量工具 | measure |  | 测量距离 |

### 4. 建议的工具分组结构

```
选择工具组 (Selection)
├── 选择工具 (V)
├── 选座工具 (X)
└── 选择刷 (C)

绘制工具组 (Drawing)
├── 焦点工具 (F)
├── 矩形区域 (S)
├── 多边形区域 (P)
├── 圆桌工具 (E)
└── 椭圆工具

座位工具组 (Seating)
├── 单个座位
├── 行座位
└── 线座位

编辑工具组 (Editing)
├── 节点编辑
└── 测量工具

视图工具组 (View)
└── 平移工具 (Space)
```

## 实现建议

### 1. 添加缺失的座位绘制工具
在 PanelLeft.tsx 中新增座位工具组，放置在绘制工具组之后。

### 2. 添加多边形工具
作为矩形区域的补充，用于创建不规则形状区域。

### 3. 键盘快捷键统一
- 使用单字母快捷键
- 避免与浏览器默认快捷键冲突
- 参考 seats.io 的快捷键设计

## 优先级建议

1. **高优先级**: 座位绘制工具 (single/row/line) - 核心功能
2. **中优先级**: 多边形工具 - 扩展区域绘制能力
3. **低优先级**: 编辑工具 - 高级功能
